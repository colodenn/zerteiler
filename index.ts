/**
 * This functions takes in a unfinished or complete json string and returns a json object.
 *
 * @param input
 *
 * @returns
 */
export const zerteiler = (input: string) => {
  console.log(`Zerteiler: ${input}`);
};
